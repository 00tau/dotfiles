text/plain; ~/.dotfiles/mutt_bgrun luakit %s; test=test -n "$DISPLAY"
text/html; ~/.dotfiles/mutt_bgrun luakit %s; test=test -n "$DISPLAY"; nametemplate=%s.html
text/html; w3m -I %{charset} -T text/html -dump
text/html; w3m -I %{charset} -T text/html -dump; copiousoutput
application/pdf; ~/.dotfiles/mutt_bgrun evince %s; test=test -n "$DISPLAY"
application/pdf; pdftotext %s -
application/pdf; pdftotext %s -; copiousoutput
#application/x-tex; ~/.dotfiles/mutt_bgrun gvim %s; test=test -n "$DISPLAY"
application/x-tex; vim -R -
application/x-tex; cat; copiousoutput
image/*; ~/.dotfiles/mutt_bgrun gpicview %s; test=test -n "$DISPLAY"
